## 20260630-升级优化`SSHFS`

### 问题描述

`SSHFS 2.x`缺少`max_conns`参数，在并行读取多文件时受单连接限制，因此考虑下载安装`SSHFS 3.x`并对挂载选项作进一步调优。

### 安装过程

安装`libfuse`：
```bash
>>> mkdir -p $HOME/software/fuse && cd $HOME/software/fuse
>>> wget https://github.com/libfuse/libfuse/releases/download/fuse-3.18.2/fuse-3.18.2.tar.gz
>>> tar -xzvf fuse-3.18.2.tar.gz

>>> cd $HOME/software/fuse/fuse-3.18.2
>>> meson setup build --prefix=$HOME/software/fuse -Duseroot=false -Dinitscriptdir= -Dudevrulesdir=$HOME/software/fuse/lib/udev/rules.d
>>> ninja -C build
>>> ninja -C build install
```

对于其中的`meson setup build --prefix=$HOME/software/fuse -Duseroot=false -Dinitscriptdir= -Dudevrulesdir=$HOME/software/fuse/lib/udev/rules.d`：
- `-Duseroot=false`禁止修改所属为`root`；
- `-Dinitscriptdir=`将初始化脚本的安装路径留空，避免写入系统目录；
- `-Dudevrulesdir=$HOME/software/fuse/lib/udev/rules.d`将`udev`规则文件安装到用户目录（该文件实际没用），防止流程因权限不足而失败。

验证安装完成：
```bash
>>> PKG_CONFIG_PATH=$HOME/software/fuse/lib64/pkgconfig pkg-config --modversion fuse3
    3.18.2
```

安装`SSHFS`：
```bash
>>> mkdir -p $HOME/software/sshfs && cd $HOME/software/sshfs
>>> wget https://github.com/libfuse/sshfs/releases/download/sshfs-3.7.6/sshfs-3.7.6.tar.xz
>>> tar -xJvf sshfs-3.7.6.tar.xz

>>> export PKG_CONFIG_PATH="$HOME/software/fuse/lib64/pkgconfig:$CONDA_PREFIX/lib/pkgconfig"

>>> cd $HOME/software/sshfs/sshfs-3.7.6
>>> meson setup build --prefix=$HOME/software/sshfs
>>> ninja -C build
>>> ninja -C build install
```

验证安装完成：
```bash
>>> /public/home/XiaAnRen/software/sshfs/bin/sshfs --version
    SSHFS version 3.7.6
    FUSE library version 3.18.2
    using FUSE kernel interface version 7.45
    fusermount3 version: 3.18.2
```

### 参数调优

使用场景：
- 数十`TB`级别的数据，传输会消耗大量时间与空间（因此不考虑`rsync`）；
- 避免在远端服务器上进行高权限配置（因此不考虑`NFSv4 over SSH`）；
- 在`mgt`上同时读取`mgt`与`admin`两个服务器上的文件进行数据处理并最终保存到`mgt`；
- 远端数据只读；
- 远端文件可能会有变动，但不会太频繁；
- 避免每次数据处理开始前都要重新挂载一次。

#### 默认参数

`SSH`默认参数：
```bash
>>> ssh -G admin | grep -E "compression|serveralive|ciphers|connecttimeout"
    compression no
    compressionlevel 6
    serveralivecountmax 3
    serveraliveinterval 0
    ciphers chacha20-poly1305@openssh.com,aes128-ctr,aes192-ctr,aes256-ctr,aes128-gcm@openssh.com,aes256-gcm@openssh.com,aes128-cbc,aes192-cbc,aes256-cbc
    connecttimeout none
```

`SSHFS`默认参数：
```bash
>>> /public/home/XiaAnRen/software/sshfs/bin/sshfs -h
    usage: /public/home/XiaAnRen/software/sshfs/bin/sshfs [user@]host:[dir] mountpoint [options]

        -h   --help            print help
        -V   --version         print version
        -f                     foreground operation
        -s                     disable multi-threaded operation
        -p PORT                equivalent to '-o port=PORT'
        -C                     equivalent to '-o compression=yes'
        -F ssh_configfile      specifies alternative ssh configuration file
        -1                     equivalent to '-o ssh_protocol=1'
        -o opt,[opt...]        mount options
        -o reconnect           reconnect to server
        -o delay_connect       delay connection to server
        -o sshfs_sync          synchronous writes
        -o no_readahead        synchronous reads (no speculative readahead)
        -o sync_readdir        synchronous readdir
        -d, --debug            print some debugging information (implies -f)
        -o dir_cache=BOOL      enable caching of directory contents (names,
                               attributes, symlink targets) {yes,no} (default: yes)
        -o dcache_max_size=N   sets the maximum size of the directory cache (default: 10000)
        -o dcache_timeout=N    sets timeout for directory cache in seconds (default: 20)
        -o dcache_{stat,link,dir}_timeout=N
                               sets separate timeout for {attributes, symlinks, names}
        -o dcache_clean_interval=N
                               sets the interval for automatic cleaning of the
                               cache (default: 60)
        -o dcache_min_clean_interval=N
                               sets the interval for forced cleaning of the
                               cache if full (default: 5)
        -o direct_io           enable direct i/o
        -o workaround=LIST     colon separated list of workarounds
                none             no workarounds enabled
                [no]rename       fix renaming to existing file (default: off)
                [no]renamexdev   fix moving across filesystems (default: off)
                [no]truncate     fix truncate for old servers (default: off)
                [no]buflimit     fix buffer fillup bug in server (default: off)
                [no]fstat        always use stat() instead of fstat() (default: off)
                [no]createmode   always pass mode 0 to create (default: off)
        -o idmap=TYPE          user/group ID mapping (default: none)
                none             no translation of the ID space
                user             only translate UID/GID of connecting user
                file             translate UIDs/GIDs contained in uidfile/gidfile
        -o uidfile=FILE        file containing username:remote_uid mappings
        -o gidfile=FILE        file containing groupname:remote_gid mappings
        -o nomap=TYPE          with idmap=file, how to handle missing mappings
                ignore           don't do any re-mapping
                error            return an error (default)
        -o ssh_command=CMD     execute CMD instead of 'ssh'
        -o ssh_protocol=N      ssh protocol to use (default: 2)
        -o sftp_server=SERV    path to sftp server or subsystem (default: sftp)
        -o directport=PORT     directly connect to PORT bypassing ssh
        -o passive             communicate over stdin and stdout bypassing network
        -o disable_hardlink    link(2) will return with errno set to ENOSYS
        -o transform_symlinks  transform absolute symlinks to relative
        -o contain_symlinks    reject absolute symlinks and symlinks containing ..
                               (enabled by default; disable with no_contain_symlinks)
        -o no_contain_symlinks allow all symlink targets including absolute and ..
        -o follow_symlinks     follow symlinks on the server
        -o no_check_root       don't check for existence of 'dir' on server
        -o password_stdin      read password from stdin (only for pam_mount!)
        -o max_conns=N         open parallel SSH connections
        -o vsock=CID:PORT      connect to the given vsock
        -o SSHOPT=VAL          ssh options (see man ssh_config)

    FUSE Options:
        -o kernel_cache        cache files in kernel
        -o [no]auto_cache      enable caching based on modification times (off)
        -o no_rofd_flush       disable flushing of read-only fd on close (off)
        -o umask=M             set file permissions (octal)
        -o fmask=M             set file permissions (octal)
        -o dmask=M             set dir  permissions (octal)
        -o uid=N               set file owner
        -o gid=N               set file group
        -o entry_timeout=T     cache timeout for names (1.0s)
        -o negative_timeout=T  cache timeout for deleted names (0.0s)
        -o attr_timeout=T      cache timeout for attributes (1.0s)
        -o ac_attr_timeout=T   auto cache timeout for attributes (attr_timeout)
        -o noforget            never forget cached inodes
        -o remember=T          remember cached inodes for T seconds (0s)
        -o modules=M1[:M2...]  names of modules to push onto filesystem stack
        -o allow_other         allow access by all users
        -o allow_root          allow access by root
        -o auto_unmount        auto unmount on process termination
        -o io_uring            enable io-uring
        -o io_uring_q_depth=<n> io-uring queue depth

    Options for subdir module:
        -o subdir=DIR	    prepend this directory to all paths (mandatory)
        -o [no]rellinks	    transform absolute symlinks to relative

    Options for iconv module:
        -o from_code=CHARSET   original encoding of file names (default: UTF-8)
        -o to_code=CHARSET     new encoding of the file names (default: UTF-8)
```

#### 性能测试

网络延迟低：
```bash
>>> ping -c 3 10.255.251.74
    PING 10.255.251.74 (10.255.251.74) 56(84) bytes of data.
    64 bytes from 10.255.251.74: icmp_seq=1 ttl=63 time=0.185 ms
    64 bytes from 10.255.251.74: icmp_seq=2 ttl=63 time=0.178 ms
    64 bytes from 10.255.251.74: icmp_seq=3 ttl=63 time=0.202 ms

    --- 10.255.251.74 ping statistics ---
    3 packets transmitted, 3 received, 0% packet loss, time 2000ms
    rtt min/avg/max/mdev = 0.178/0.188/0.202/0.015 ms
>>> sshping -H admin
    ssh-Login-Time:               4.18  s
    Minimum-Latency:               227 us
    Median-Latency:                269 us
    Average-Latency:               265 us
    Average-Deviation:            32.7 us
    Maximum-Latency:               803 us
    Echo-Count:                  1.00 kB
    Upload-Size:                 8.00 MB
    Upload-Rate:                 20.5 MB/s
    Download-Size:               8.00 MB
    Download-Rate:                840 kB/s
```

`mgt`与`SSH`都支持`AES-NI`硬件加速，因此`aes128-gcm`加密算法的大文件吞吐量更优：
```bash
>>> lscpu | grep -o aes
    aes
>>> ssh -Q cipher | grep "aes128-gcm@openssh.com"
    aes128-gcm@openssh.com

>>> sudo /public/home/XiaAnRen/software/sshfs/bin/sshfs admin:/data /data3/XiaAnRen/admin-data \
>>>      -F /public/home/XiaAnRen/.ssh/config -o IdentityFile=/public/home/XiaAnRen/.ssh/id_rsa -o ro,allow_other,reconnect \
>>>      -o Ciphers=chacha20-poly1305@openssh.com
>>> dd if=/data3/XiaAnRen/admin-data/ERA5_LAND/2020/land.20200202.nc of=/dev/null bs=1M
    1179382200 bytes (1.2 GB) copied, 13.408 s, 88.0 MB/s
>>> sudo /public/home/XiaAnRen/software/fuse/bin/fusermount3 -u /data3/XiaAnRen/admin-data

>>> sudo /public/home/XiaAnRen/software/sshfs/bin/sshfs admin:/data /data3/XiaAnRen/admin-data \
>>>      -F /public/home/XiaAnRen/.ssh/config -o IdentityFile=/public/home/XiaAnRen/.ssh/id_rsa -o ro,allow_other,reconnect \
>>>      -o Ciphers=aes128-gcm@openssh.com
>>> dd if=/data3/XiaAnRen/admin-data/ERA5_LAND/2020/land.20200202.nc of=/dev/null bs=1M
    1179382200 bytes (1.2 GB) copied, 12.8148 s, 92.0 MB/s
```

#### 最终方案

```bash
>>> # mgt
>>> sudo /public/home/XiaAnRen/software/sshfs/bin/sshfs admin:/data /data3/XiaAnRen/admin-data \
>>>      -F /public/home/XiaAnRen/.ssh/config -o IdentityFile=/public/home/XiaAnRen/.ssh/id_rsa -o ro,allow_other,reconnect \
>>>      -o ServerAliveInterval=15,ServerAliveCountMax=3 -o Ciphers=aes128-gcm@openssh.com -o auto_cache,max_conns=8
>>> sudo /public/home/XiaAnRen/software/fuse/bin/fusermount3 -u /data3/XiaAnRen/admin-data
>>> # admin
>>> sudo /public/home/XiaAnRen/software/sshfs/bin/sshfs mgt:/data4 /data/mgt-data4 \
>>>      -F /public/home/XiaAnRen/.ssh/config -o IdentityFile=/public/home/XiaAnRen/.ssh/id_rsa -o ro,allow_other,reconnect \
>>>      -o ServerAliveInterval=15,ServerAliveCountMax=3 -o Ciphers=aes128-gcm@openssh.com -o auto_cache,max_conns=8
>>> sudo /public/home/XiaAnRen/software/fuse/bin/fusermount3 -u /data/mgt-data4
```

对于其中的`-o ServerAliveInterval=15,ServerAliveCountMax=3 -o Ciphers=aes128-gcm@openssh.com -o auto_cache,max_conns=8`：
- `ServerAliveInterval=15,ServerAliveCountMax=3`定期监测并判定失效连接，配合`reconnect`保证自动重连，实现长期稳定挂载；
- `Ciphers=aes128-gcm@openssh.com`更好利用`AES-NI`硬件加速；
- `auto_cache`基于文件时间戳与大小自动失效缓存；
- `max_conns=8`开启并行连接，提升同时读取多个不同文件时的整体吞吐与系统响应。
